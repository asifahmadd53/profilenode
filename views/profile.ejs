<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="bg-slate-900 text-white min-h-screen w-full p-10">
    

            <div class="flex items-center justify-between">
                <h3 class="text-3xl ">Hii, <%= user.name %></h3>
                <button class="bg-red-800 p-2 rounded-md"><a class="inline-block" href="/logout">Logout</a></button>
            </div>

            
            <div class="my-4 profile w-12 h-12 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            
            

            

            <h5>You can crate a new post.</h5>
            <form class="" action="/post" method="post">
                <textarea placeholder="Whats in your mind?" class="w-1/3 h-52 resize-none bg-transparent border outline-none border-zinc-600 block" name="content" id=""  ></textarea>
                <input class="bg-blue-700 m-3 p-2 rounded-md text-centerw text-white" type="submit" value="Submit ">
            </form>
        <div class="mt-20">
            <h3 class="text-2xl ">Your posts</h3>
            <div class="mt-5 flex flex-col gap-4">
                <% user.post.reverse().forEach(function(post) { %>
                  <div class="post w-1/2 border p-4">
                    <h2 class="text-2xl font-bold pb-3">@<%= user.username%></h2>
                    <p class="pb-3"><%= post.content %></p>
                    <small ><%= post.likes.length %></small>
                    <div class="flex justify-between px-5">
                      <a class="text-blue-600" href="/like/<%= post._id %>"><%= post.likes.indexOf(user._id) ? "Like" : "unLike" %></a>
                      <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                  </div>
                <% }) %>
              </div>
        </div>
    </div>
</body>
</html>